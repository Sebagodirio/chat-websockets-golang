# chat-websockets-golang
Un simple chat usando websocket con golang

## Como empezar
```
go mod tidy
```
> Para instalar las dependencias necesarias

- Luego pararse en la raiz del proyecto y correr:
`go run cmd/web/main.go`
